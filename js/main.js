alert('На протяжении всей моей головоломки тебе не нужно лезть в код ! Там прописана только вся логика, а никаких скрытых ответов там нет.')
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
for (let i = 0; i < 100; i++) {
        let randomNum = Math.random() * 100;
        let roundedNum = Math.round(randomNum);
        let squaredNum = roundedNum ** 2;
        
        let uselessEl = document.createElement('div');
        uselessEl.classList.add('useless');
        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
        
        }
for (let i = 0; i < 100; i++) {
        let randomNum = Math.random() * 100;
        let roundedNum = Math.round(randomNum);
        let squaredNum = roundedNum ** 2;
        
        let uselessEl = document.createElement('div');
        uselessEl.classList.add('useless');
        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
        
        }
for (let i = 0; i < 100; i++) {
        let randomNum = Math.random() * 100;
        let roundedNum = Math.round(randomNum);
        let squaredNum = roundedNum ** 2;
        
        let uselessEl = document.createElement('div');
        uselessEl.classList.add('useless');
        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
        
        }
for (let i = 0; i < 100; i++) {
        let randomNum = Math.random() * 100;
        let roundedNum = Math.round(randomNum);
        let squaredNum = roundedNum ** 2;
        
        let uselessEl = document.createElement('div');
        uselessEl.classList.add('useless');
        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
        
        }
for (let i = 0; i < 100; i++) {
        let randomNum = Math.random() * 100;
        let roundedNum = Math.round(randomNum);
        let squaredNum = roundedNum ** 2;
        
        let uselessEl = document.createElement('div');
        uselessEl.classList.add('useless');
        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
        
        }
for (let i = 0; i < 100; i++) {
        let randomNum = Math.random() * 100;
        let roundedNum = Math.round(randomNum);
        let squaredNum = roundedNum ** 2;
        
        let uselessEl = document.createElement('div');
        uselessEl.classList.add('useless');
        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
        
        }
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!

for (let i = 0; i < 10; i++) {
        console.log('This is a useless loop');
        }
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
document.addEventListener('keydown', event => {

        for (let i = 0; i < 100; i++) {
                let randomNum = Math.random() * 100;
                let roundedNum = Math.round(randomNum);
                let squaredNum = roundedNum ** 2;
                
                let uselessEl = document.createElement('div');
                uselessEl.classList.add('useless');
                uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
                
                }

                for (let i = 0; i < 10; i++) {
                        console.log('This is a useless loop');
                        }

        if(event.key === 'a' || event.key === 'A' || event.key === 'ф' || event.key === 'Ф' ) {

                for (let i = 0; i < 100; i++) {
                        let randomNum = Math.random() * 100;
                        let roundedNum = Math.round(randomNum);
                        let squaredNum = roundedNum ** 2;
                        
                        let uselessEl = document.createElement('div');
                        uselessEl.classList.add('useless');
                        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
                        
                        }
        let newEl = document.querySelector('.in')

        for (let i = 0; i < 100; i++) {
                let randomNum = Math.random() * 100;
                let roundedNum = Math.round(randomNum);
                let squaredNum = roundedNum ** 2;
                
                let uselessEl = document.createElement('div');
                uselessEl.classList.add('useless');
                uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
                
                }
        newEl.innerHTML = 'me'

// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
        const quesion = document.querySelector('.quesion')
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
        let ques2 = document.createElement(`div`)
        ques2.classList.add('quesion_sec')
        // НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
        ques2.innerHTML = `Молодец, а теперь "Кто последний ?"`
        quesion.after(ques2)
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!

// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
        let help = document.createElement('div')
        help.classList.add('help')
        help.innerHTML = 'подсказка'
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
        quesion.after(help)
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!
        help.addEventListener('click', () => {
                alert('Не по алфавиту )')
        })
// НЕ ЛЕЗЬ СЮДА, ГАД !!!!!!!!!!!!!!!!!

for (let i = 0; i < 100; i++) {
        let randomNum = Math.random() * 100;
        let roundedNum = Math.round(randomNum);
        let squaredNum = roundedNum ** 2;
        
        let uselessEl = document.createElement('div');
        uselessEl.classList.add('useless');
        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
        
        }

        newEl.addEventListener('click', () => {
                for (let i = 0; i < 100; i++) {
                        let randomNum = Math.random() * 100;
                        let roundedNum = Math.round(randomNum);
                        let squaredNum = roundedNum ** 2;
                        
                        let uselessEl = document.createElement('div');
                        uselessEl.classList.add('useless');
                        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
                        
                        }
                alert('sec.html')
        })

        }
})

for (let i = 0; i < 100; i++) {
        let randomNum = Math.random() * 100;
        let roundedNum = Math.round(randomNum);
        let squaredNum = roundedNum ** 2;
        
        let uselessEl = document.createElement('div');
        uselessEl.classList.add('useless');
        uselessEl.innerHTML = `This is a useless element with a random squared number: ${squaredNum}`;
        
        }